An executable file “ProteinEvolverABC_GUI.jar" is included and it should be launched (java -jar ProteinEvolverABC_GUI.jar) without problems on any OS.

Alternatively, the source code can be compiled with the Makefile:
Type "make all" to compile.
Type "make clean" to remove compiled files.

